{
  "name": "intake-investigations",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A deeply unnecessary noir detective patient intake game.",
  "scripts": { "start": "node server.mjs", "test": "node --test test/*.test.mjs" },
  "engines": { "node": ">=22" }
}
